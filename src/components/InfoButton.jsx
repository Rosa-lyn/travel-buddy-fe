import React from "react";
import InfoPopup from "./InfoPopup";

const InfoButton = () => {
  return (
    <section>
      {/*  opens info popup */}
      <button>i</button>
      <InfoPopup />
    </section>
  );
};

export default InfoButton;
