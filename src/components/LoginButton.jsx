import React from "react";
import LoginForm from "./LoginForm";

const LoginButton = () => {
  return (
    <section>
      {/*  opens login page */}
      <button>login</button>
      <LoginForm />
    </section>
  );
};

export default LoginButton;
