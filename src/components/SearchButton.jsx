import React from 'react';
import { SearchButton } from '../styles/headerStyles';


const Search = () => {
  return (
    <SearchButton>
      <i class="fa fa-search"></i>
    </SearchButton>
  );
};

export default Search;